---
description: Scan project for PII leaks, missing classification, missing audit log
---

Invoke the `audit-pii` skill on the path provided as `$ARGUMENTS`, or whole project if empty.

Report findings in CRITICAL / HIGH / MEDIUM / OK format. Apply fixes only with explicit user OK per item.
