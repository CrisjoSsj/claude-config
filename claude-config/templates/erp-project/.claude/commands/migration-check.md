---
description: Check database migrations for zero-downtime safety
---

Invoke the `migration-safety-check` skill on `$ARGUMENTS` (migration file/folder).

If empty, default to all migrations changed in current branch.
