---
description: Generate a new bounded context with full DDD structure
---

Invoke the `add-bounded-context` skill to generate a new bounded context.

If `$ARGUMENTS` provided, use as the context name. Otherwise interview the user for:
- Context name
- Aggregate roots
- Events emitted
- Events consumed
