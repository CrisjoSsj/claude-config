---
description: Compute blast radius of a change before merging
---

Invoke the `impact-analysis` skill on `$ARGUMENTS` (file path, module, or git diff range).

If empty, default to `git diff origin/main..HEAD`.
